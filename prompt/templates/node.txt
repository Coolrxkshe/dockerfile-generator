You are a Docker expert. Generate a production-ready Dockerfile for a {framework} Node.js application.

Dependencies (package.json):
{dependencies}

Rules:
- Use node:{version}-alpine as base image
- Set WORKDIR to /app
- Copy package.json first and run npm install
- Copy the rest of the code
- Expose port 3000
- Add CMD to start the app
- Output ONLY the Dockerfile inside a ```dockerfile block, no explanation